# 変更履歴

実装内容を確認できる Ver.1.3.3 から変更点を記録します。それ以前のバージョン番号と記録上の更新日時は `programs/global_initializer.js` を参照してください。Ver.1.3.3の時刻は Git のリリースコミット時刻（日本標準時）、Ver.1.4.0以降は各更新作業日の日本標準時です。

## Ver.1.4.3 — 2026-09-17（JST）

### 図鑑

- 図鑑カタログ生成を `encyclopedia_catalog.js` に一本化。`meta_progress.js` のカタログを `achievements.js` が後から上書きする二重構造を実行時の最終参照から外し、表示・解放・図鑑完成実績が同じカタログIDを使用するよう変更。
- 食料・武器・道具・素材の各カテゴリが実データから1件以上生成できることをゲーム起動前に検証し、空カタログのまま図鑑画面へ進まないようにした。
- 同名項目のID正規化、同名素材の採取道具統合、「女の子……？」3段階の固定IDを図鑑カタログ側で一元管理。
- 図鑑画面を `Toho_archive_scene_v143` に切り替え、表示直前の所持品走査と解放ID変化時の再描画を、後付けprototypeラッパーではなく実際にGameAppが生成するシーンクラスへ統合。
- 実データを使ったカテゴリ生成・ID正規化・所持品からの解放復元と、図鑑シーンの再描画を回帰テストに追加。

### 保存・互換性

- 1.4.xではCookie互換を廃止。進行・設定・アセットバージョン判定を `localStorage` のみに統一し、旧 `playingdata` / `storydata` / BGM / SE / version Cookieを読み込まない。
- 旧Cookieから `localStorage` への自動移行とCookieへのフォールバックを削除。1.4.xの既存 `localStorage` セーブは引き続き使用する。
- `index.html` とゲーム内部のバージョンを `1.4.3` に更新し、1.4.2を取得済みのブラウザでも新しいスクリプト構成へ更新されるようにした。

## Ver.1.4.2 — 2026-09-17（JST）

### 図鑑・データ読み込み

- JSONローダーを、リクエスト開始時点で完了扱いにする旧実装から、HTTP取得・JSON解析・ゲーム内オブジェクトへの変換完了まで状態を追跡するPromiseベースの実装へ変更。
- 食料・武器・道具・素材・レシピ・敵・ストーリーの全JSONが読み込み・変換済みになってから `GameApp` を生成するよう変更。図鑑や探索が空配列を参照した状態で起動する競合を防止。
- 図鑑表示時にも現在の所持品を再走査し、過去の取得フック取りこぼしがあるセーブでも保持中アイテムの解放情報を復元する。
- JSON読み込み失敗時は空データのままゲームを開始せず、失敗したデータ種別を表示して起動を中止するよう変更。
- リポジトリ実データを使ったJSON変換テストと、所持品走査・アイテム取得・敵遭遇による図鑑解放の永続保存テストを追加。

### バージョン・キャッシュ

- `global_initializer.js` と `index.html` のバージョンを `1.4.2` に更新。
- Ver.1.4.1のまま追加修正していたため、既に1.4.1を開いたブラウザが新しい `index.html` を同一バージョンとして扱い、修正版スクリプト構成へ切り替わらない経路があった。パッチバージョンを更新して既存のバージョン確認・キャッシュ更新処理を確実に発火させる。

## Ver.1.4.1 — 2026-09-17（JST）

### セーブ

- プレイ進行状況をCookieの `playingdata`・`storydata` から `localStorage` の `the_toho:progress:v1` へ移行。プレイヤーデータとストーリー進行番号を1レコードとして保存する。
- v1.4.0以前の `playingdata`・`storydata` Cookieが残っている場合は、初回の復元時に新形式へ移行し、`localStorage` への保存成功後に旧Cookieを削除する。新規プレイでは旧進行Cookieを書き込まない。
- `localStorage` 内の新形式データが破損している場合は、旧Cookieへ暗黙に巻き戻さず、既存データを上書き・削除せずタイトル画面に留まる。
- Cookieの1項目あたりの容量制約により、所持品増加後に `playingdata` が更新されなくなる問題を回避。4KBを超える進行データ、旧Cookie移行、復元、削除を確認する回帰テストを追加。
- HTML・ゲーム内部のバージョンを `1.4.1` に更新し、保存モジュールをキャッシュ更新対象に追加。

### 図鑑

- 図鑑画面の更新判定が総項目数だけを見ていたため、解放IDだけが変化した場合に解放数や `？？？` 表示が更新されない問題を修正。解放IDを含む状態シグネチャを監視して一覧・トップを再描画する。
- 同一バージョン内の修正でも古いJavaScriptを参照しないよう、スクリプトのリビジョン文字列を更新。

## Ver.1.4.0 — 2026-09-17（JST）

Ver.1.3.3からの変更点。正式公開は `feature/1.4.0` を `master` にマージした時点とします。

### 背景・画面表示

- タイトル、昼夜の拠点、探索・入手・戦闘、リザルト、記録・図鑑、ストーリーに背景画像を追加。探索系は到達距離0・50・100kmで背景を切り替え、ストーリーは共通画像を基本に個別背景も指定できるようにした。
- GitHub Pagesのサブパスを考慮してアセットを読み込み、背景の読み込み失敗時は従来の背景色へフォールバック。背景画像上で文字が読めるように暗いオーバーレイを使用。
- タイトルに実績、ストーリー、図鑑、プレイ履歴の入口を整備。画面枠、バージョン表記、一覧の縦スクロール・余白やスクロール位置の維持を調整。

### 図鑑・記録

- 食料・武器・道具・素材・敵の5種類の図鑑を追加。初入手・初遭遇による解放、解放数・率、詳細情報の表示と確認付き削除に対応。
- 同名素材は図鑑上で1種類として数え、既存の発見IDを移行。表示名が同じ「女の子……？」3体は内部IDと図鑑項目をそれぞれ区別。
- 最高到達距離・最高生存日数と直近10件の死亡時プレイ履歴を保存。履歴詳細に戦績、死因、所持品を表示し、戦闘回数は勝利数として記録。履歴削除時は最高記録も初期化する。

### 実績・ストーリー

- 89件の実績を追加。周回内と累計の距離・日数、気力、行動連続数、単発被ダメージ、アイテム入手、敵討伐、カテゴリ別・全図鑑完成を条件として判定。通常／シークレット表示と解放済み優先の一覧、確認付き削除に対応。
- 実績解除ポップアップと専用効果音 `notice.mp3` を追加。複数解除した際は通知を順番に表示し、表示ごとに効果音を再生。
- 既存4話を「遭遇」「変容」「予感」「異変」として管理。最初の3話はそれぞれ対応する「女の子……？」の討伐、4話は3段階すべての討伐後にさらにいずれかを討伐すると解放。
- ストーリーの解放・既読状態を永続保存。戦闘後への自動挿入、勝利画面への復帰、別プレイでの再挿入、タイトルからの再読、確認付き削除に対応。実績・ストーリーの判定条件は定義データとイベント処理を分離し、後から追加できる構成にした。

### セーブ・操作・検証

- 最高記録・図鑑・実績・ストーリーと周回中の一時データを分けて保存。履歴の重複記録や累計値の二重加算、保存失敗時のデータ消去を防ぐための処理を追加。旧プレイCookieの復元にも対応。
- 制作画面のレシピ一覧、詳細・履歴のスクロールや、武器が破損したときの攻撃候補表示を修正。データ削除の確認文は対象・保持されるデータ・取り消し不可を明示するように統一。
- 実績、図鑑完成、ストーリー、履歴、削除確認と通知音のブラウザ非依存テストを追加し、GitHub Actionsで実行。実ブラウザでの全シーン通し操作は未検証。

## Ver.1.3.3 — 2026-09-16 17:40 JST

リリースコミット: [`ddb553b129c90c73175c7fed58fdc8daf661c9f8`](https://github.com/surumeneco/the_toho/commit/ddb553b129c90c73175c7fed58fdc8daf661c9f8)

### アイテム・制作

- 鹿肉カレーの必要素材を「気力の欠片 ×1」から「鹿肉 ×1」に変更。
- 「森羅」を素材カテゴリに登録（制作のみ）。
- 金床の制作レシピに「鉄の槍」を追加。制作気力15、制作数1、必要素材は木の枝×3・ツタ×5・鉄×5・毛皮×2。

### バージョン確認・アセット更新

- HTMLとゲーム初期化処理のバージョンを `1.3.3` に更新し、バージョンを `version` Cookieに保存。
- 起動時に `index.html` をキャッシュを使用せず確認し、新しいバージョンを検知するとそのバージョンをURLに付けて再読み込みする処理を追加。
- 保存されたバージョンが存在しない、または現在のバージョンと異なる場合、ゲームのURL配下に対応するCacheStorage内のキャッシュを削除。JavaScript・JSON・音声の参照URLにもバージョンを付け、旧版のHTTPキャッシュの参照を回避。

### セーブ・音量設定

- ゲーム進行状況（`storydata`・`playingdata`）と設定（`BGM`・`SE`・`version`）のCookieを分離。ゲームオーバーや「諦める」では進行状況のみ削除し、音量・バージョン情報は維持。
- 音量設定をページ読み込み時に復元し、変更時に保存。Cookieを保存順ではなく名前で取得する方式へ変更。
- 保存済みプレイデータの読み込み時に、JSON化で失われる武器攻撃力のダイスメソッドを復元。

※ ブラウザ全体のHTTPキャッシュを消去する機能ではありません。ゲーム配下のCacheStorage削除と、バージョン付きURLによるアセット更新を行います。